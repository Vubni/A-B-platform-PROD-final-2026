[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -ra
markers =
    event_types: Event Types API (list, create, get, update, archive)
    events_submit: Events Submit API (batch submission)

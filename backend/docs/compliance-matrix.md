# Матрица соответствия заданию (B7-3)

Полная матрица трассируемости «задание → критерий → реализация → проверка».  
Файл предназначен для быстрого поиска реализации и сценариев проверки по каждому критерию.

**Важно для проверки B3:** в реализации статус «на ревью» именуется **on_review** (в ТЗ — in_review); семантика та же.

| ID задания | ID критерия | Проблема/риск | Где реализовано | Как проверяется | Какие данные нужны | Статус |
|-----------|-------------|---------------|-----------------|-----------------|--------------------|--------|
| 1 | `B2-1` | При отсутствии активного эксперимента система должна возвращать `default`, иначе ломается контрольный сценарий и сравнение вариантов. | `backend/functions/decide.py::get_decisions_for_subject` — ветка `if not experiments:` и финальный проход по флагам без эксперимента. | Интеграционный тест `tests/test_decide_api.py::test_decide_returns_default_value_when_no_experiment`; ручной `POST /api/v1/decide` c флагом без экспериментов. | Флаг с `default_value` (`/api/v1/flags`), без активных экспериментов; субъект `subject-test-001`. | `готово` |
| 2 | `B2-2` | Пользователь вне таргетинга не должен попадать в эксперимент и получать вариант. | `backend/functions/decide.py::get_decisions_for_subject` — проверка `evaluate_targeting_rule`, при `False` записывается решение с `default_value` и `experiment=None`. | Тест в `tests/test_decide_api.py` (сценарий с таргетингом); демо: создать эксперимент с таргетингом и вызвать `POST /api/v1/decide` с неподходящими `attributes`. | Эксперимент с `targeting_rule`, два субъекта: один попадает по таргетингу, второй — нет. | `готово` |
| 3 | `B2-4` | При неизменной конфигурации результат для одного субъекта должен быть детерминированным, иначе невозможно корректно считать эффекты. | `backend/functions/decide.py` — поиск последнего решения по `(subject_id, flag_id)` и повторное использование значения (ветка `existing`). | Интеграционный тест `tests/test_decide_api.py::test_decide_same_subject_same_value` — два вызова решателя для одного субъекта, сравнение `flag_value`. | Один флаг, один эксперимент, один `subject_id`; несколько запросов `POST /api/v1/decide`. | `готово` |
| 4 | `B4-1` | Событие с неверным типом поля не должно приниматься, иначе ломается отчётность и guardrail-метрики. | `backend/functions/events_submit.py::_validate_required_params` и `_validate_single_event` — проверка типов `number/string/bool`, timestamp и UUID. | Тесты `tests/test_events_api.py::test_events_submit_invalid_body_*` и `test_events_submit_required_params_rejected`; ручной `POST /api/v1/events` с payload неправильного типа. | Зарегистрированный `event_type` с `required_params`; события с правильным/неправильным типом полей. | `готово` |
| 5 | `B4-3` | Дубликаты событий не должны менять итоговый расчёт по эксперименту. | `backend/functions/events_submit.py::_event_exists` и логика `duplicates` в `process_events_batch`. | Интеграционный тест `tests/test_events_api.py::test_events_submit_batch_mixed_accepted_rejected_duplicates`; повторная отправка события с тем же `event_id`. | Набор событий с повторяющимся `event_id`; эксперимент с привязанными решениями. | `готово` |
| 6 | `B5-3` | Превышение порога guardrail-метрики должно быть обнаружено, иначе эксперимент продолжит деградировать метрику. | `backend/functions/guardrails.py::_evaluate_experiment_guardrails` — расчёт значения по `metric_key` и сравнение с `threshold`. | Интеграционный сценарий: запустить эксперимент с guardrail-метрикой, сгенерировать события так, чтобы метрика превысила порог; проверить историю `GET /api/v1/experiments/{id}/guardrail-history`. | Эксперимент с `metric_type='guardrail'`, события для расчёта метрики, настроенный `metric_guardrails` с низким порогом. | `готово` |
| 7 | `B5-4` | После срабатывания guardrail должно выполняться действие (pause/rollback), иначе safety-механизм бесполезен. | `backend/functions/guardrails.py::_evaluate_experiment_guardrails` — ветки `if action == "pause"` и `elif action == "rollback_to_control"`. | Демонстрация: настроить guardrail с `action="pause"` и `action="rollback_to_control"`, превысить порог; проверить статус эксперимента через `GET /api/v1/experiments/{id}`. | Эксперимент с guardrail-метрикой; метрика, которая легко превышает порог при генерации событий. | `готово` |
| 8 | `B6-1` | Нужен фильтр отчёта по периоду; без него вычисления по всей истории будут дорогими и нерелевантными. | `backend/api/reports.py::reports_experiment` + `backend/functions/reports.py::get_experiment_report` — параметры `start`/`end` и фильтрация по ним. | Тест `tests/test_reports_api.py::test_report_invalid_window`; ручной вызов `GET /api/v1/experiments/{id}/report?start=...&end=...`. | Эксперимент с событиями внутри и вне окна; два вызова отчёта с разными `start`/`end`. | `готово` |
| 9 | `B6-2` | Отчёт должен показывать показатели в разрезе вариантов, иначе нельзя сравнивать A/B. | `backend/functions/reports.py::get_experiment_report` — формирование массива `variants` с отдельными `metric_values` и `event_counts` по каждому варианту. | Демонстрация `GET /api/v1/experiments/{id}/report` на эксперименте с несколькими вариантами; визуальная проверка разреза по вариантам. | Эксперимент с минимум двумя вариантами, события, которые генерируют отличающиеся метрики по вариантам. | `готово` |
| 10 | `B9-1/B9-2` | Без health/readiness нельзя безопасно включить сервис в балансировщик и понимать его состояние. | `backend/api/health.py` и регистрация маршрутов в `backend/server.py` (`/health`, `/ready`). | Тесты `tests/test_health.py`; ручная проверка статусов при старте и после инициализации БД (`curl /health`, `curl /ready`). | Запущенный `docker-compose`; curl-запросы к `/health` и `/ready` до и после инициализации. | `готово` |
| 11 | B1-1 | Без предусловий среду невозможно воспроизвести. | `backend/README.md` (переменные DB_*, RANDOM_SECRET и др.), корневой README (docker compose). | Проверка наличия в документации перечня зависимостей и переменных. | — | `готово` |
| 12 | B1-2 | Нужны точные команды старта. | Корневой README: `docker compose up -d`; backend/README: локальный запуск. | Выполнение команд по инструкции. | Docker, порт 80. | `готово` |
| 13 | B1-3 | Запуск без скрытых шагов. | Инструкции не требуют неописанных действий. | Запуск по README/Runbook. | — | `готово` |
| 14 | B1-4 | Сервисы должны стартовать и быть доступны. | `docker-compose.yml`, `server.py`; `/ready` переходит в 200 после инициализации БД. | После `docker compose up -d` проверить `curl /health`, `curl /ready` (в течение 180 с). | Запущенный compose. | `готово` |
| 15 | B1-5 | Сквозной путь: выдача варианта → событие → результат. | Демо в `backend/README.md` (шаги 1–5); тесты decide/events/reports. | Выполнить шаги демо: флаг → эксперимент (ревью, запуск) → decide → events → report. | Сид `seed_test_data.py` (пользователи, флаг, event_types, метрики, approver_group); пакет сценариев — `backend/docs/demo-scenarios.md`. | `готово` |
| 16 | B2-3 | При применимом эксперименте должен возвращаться вариант. | `backend/functions/decide.py` — ветка с выбором варианта по весам, возврат `variant_value` и experiment_id/variant. | Тест с активным экспериментом и субъектом в таргетинге; проверка `flag_value` и `experiment`. | Флаг, эксперимент в статусе running, субъект в таргетинге. | `готово` |
| 17 | B2-5 | Распределение должно учитывать веса вариантов. | `backend/functions/decide.py` — выбор варианта по дефициту (target_ratio = weight/audience_fraction). | Демо: много запросов decide с разными subject_id; сравнить доли вариантов с весами. | Эксперимент с двумя и более вариантами и заданными весами. | `готово` |
| 18 | B3-1 | Переход draft → на ревью. | `backend/functions/experiments.py::submit_review` → status = 'on_review'. В системе статус **on_review** (эквивалент in_review по ТЗ). | PATCH .../status с new_status=on_review для эксперимента в draft. | Эксперимент в draft. | `готово` |
| 19 | B3-2 | Переход на ревью → approved при достижении порога одобрений. | `add_review_record(..., "approved")` и проверка count >= min_approvals → status = 'approved'. | Отправить эксперимент на ревью, одобрить от min_approvals аппруверов, проверить status=approved. | Эксперимент on_review, пользователи с ролью approver в группе. | `готово` |
| 20 | B3-3 | Запуск без достаточных одобрений блокируется. | Переход в running только из approved; в approved только при count >= min_approvals. | Попытка перевести в running до достижения порога — статус остаётся on_review. | Эксперимент on_review, одобрений меньше min_approvals. | `готово` |
| 21 | B3-4 | Недопустимые переходы статусов блокируются. | `STATUS_TRANSITIONS` и проверка в `update_experiment_status`; триггеры БД. | Попытка недопустимого перехода (например draft → running) — ошибка. | Любой эксперимент. | `готово` |
| 22 | B3-5 | Ревью только назначенными ролями/аппруверами. | `can_review_experiment(user_id, experiment_id)` — проверка членства в approver_group. | Одобрение от пользователя не из группы аппруверов — отказ или игнор. | Эксперимент on_review, пользователь не в approver_group. | `готово` |
| 23 | B4-2 | Событие без обязательных полей отклоняется. | `backend/functions/events_submit.py::_validate_single_event` — event_id, decision_id, event_type_key, subject_id, timestamp. | Тесты test_events_submit_* с отсутствующими полями; ответ rejected и errors. | Тип события с required_params при необходимости. | `готово` |
| 24 | B4-4 | Экспозиция связана с decision_id. | Таблица `event_occurrences.decision_id` NOT NULL, FK на decisions. | Событие принимается только с валидным decision_id; в отчёте события по решениям. | Решение из POST /decide, событие с этим decision_id. | `готово` |
| 25 | B4-5 | Конверсия только при валидной экспозиции. | `requires_show_event_type_id` и `_show_event_exists` в events_submit; зависимые события учитываются после «показа». | Событие с requires_show без предшествующего show по decision_id — в очереди или не учитывается в метриках. | Типы событий с requires_show_event_type_id, пара exposure + conversion. | `готово` |
| 26 | B5-1 | Guardrail хранит metric_key. | Таблица `metric_guardrails(metric_key)`, `experiment_metrics.metric_key` при metric_type='guardrail'. | GET /api/v1/guardrails, GET эксперимент — в метриках guardrail указан metric_key. | Настроенный guardrail в каталоге метрик. | `готово` |
| 27 | B5-2 | Guardrail хранит порог. | `metric_guardrails.threshold`, используется в guardrails.py. | GET /api/v1/guardrails/{metric_key} — поле threshold. | — | `готово` |
| 28 | B5-5 | Срабатывание guardrail фиксируется в аудите. | `record_guardrail_trigger` → INSERT в experiment_guardrail_history (metric_key, threshold, window_seconds, action, metric_value, triggered_at). | GET /api/v1/experiments/{id}/guardrail-history после срабатывания. | Эксперимент с guardrail, метрика превысила порог. | `готово` |
| 29 | B5-6 | Ограничение постоянного участия в экспериментах. | MAX_ACTIVE_EXPERIMENTS_PER_SUBJECT, subject_experiment_cooldown; проверки в decide.py. | Субъект уже в max экспериментах — новый запрос получает default; после cooldown снова может попасть. | Несколько экспериментов на разных флагах, один subject_id. | `готово` |
| 30 | B6-3 | В отчёте отображаются выбранные метрики эксперимента. | `backend/functions/reports.py::get_experiment_report` — metrics из experiment_metrics, значения по вариантам. | GET .../report — в ответе metrics и variants[].metric_values. | Эксперимент с primary/auxiliary метриками, события. | `готово` |
| 31 | B6-4 | Фиксация исхода: rollout/rollback/no effect. | POST /api/v1/experiments/{id}/complete с completion_outcome, completion_comment, completion_winner_variant_id. | Завершить эксперимент с outcome rollout_winner/rollback/no_effect; проверить в GET эксперимент и в отчёте. | Эксперимент в running или paused. | `готово` |
| 32 | B6-5 | Сохранение обоснования решения. | completion_comment при complete; в отчёте completion.comment. | POST .../complete с comment; GET .../report — поле completion.comment. | — | `готово` |
| 33 | B7-1 | Понятный нейминг сущностей и интерфейсов. | Эндпоинты и сущности: flags, experiments, decide, events, report, guardrails, event_types, metrics. | Обход API по документации — названия однозначны. | — | `готово` |
| 34 | B7-2 | Явные границы модулей. | api/ (HTTP), functions/ (логика), database/ (доступ к БД). | Обзор структуры backend/README «Карта репозитория». | — | `готово` |
| 35 | B7-4 | Ключевые архитектурные решения зафиксированы. | backend/README.md — раздел «Ключевые архитектурные решения (B7-4)». | Чтение раздела и сверка с кодом. | — | `готово` |
| 36 | B7-5 | C4 Context диаграмма. | `backend/docs/c4-diagrams.md` — C4 Context: акторы (Аналитик, Approver, Viewer), система LOTTY A/B Platform. | Открыть c4-diagrams.md; проверить наличие уровня L1 (границы платформы). | — | `готово` |
| 37 | B7-6 | C4 Container диаграмма. | `backend/docs/c4-diagrams.md` — C4 Container: Backend API, PostgreSQL, взаимодействия. | Открыть c4-diagrams.md; проверить уровень L2 (контейнеры). | — | `готово` |
| 38 | B7-7 | C4 Component для критичного пути. | `backend/docs/c4-diagrams.md` — C4 Component для Backend: decide → event → report/guardrail без детализации до классов. | Открыть c4-diagrams.md; проверить уровень L3 по критичному пути. | — | `готово` |
| 39 | B7-8 | Карта репозитория. | backend/README.md — «Карта репозитория (B7-8)» и критичный поток. | Наличие раздела с деревом и описанием потока. | — | `готово` |
| 40 | B7-9 | Ограничения и упрощения зафиксированы. | backend/README.md — «Ограничения и упрощения (B7-9)». | Наличие раздела с перечнем и ссылками на код. | — | `готово` |
| 41 | B8-1 | Негативные тесты. | test_events_submit_invalid_*, test_auth_login_invalid_*, test_flags_create_invalid_key_returns_400, test_experiments_*_returns_400 и др. | Запуск pytest; наличие тестов с ожиданием 400/401/403/404. | — | `готово` |
| 42 | B8-2 | Интеграционные/контрактные тесты критичного пути. | test_decide_api, test_events_api, test_reports_api с живым API и БД. | Запуск pytest tests/test_decide_api.py tests/test_events_api.py tests/test_reports_api.py. | API_BASE_URL, сид данных. | `готово` |
| 43 | B8-4 | Отчёт по тестированию с покрытием. | tests/README.md — наборы тестов, отчёт покрытия эндпоинтов (список + %). | Выполнение pytest из README; в конце — блок «Покрытие эндпоинтов» (протестированные эндпоинты и % от всех). | — | `готово` |
| 44 | B9-3 | Экспорт базовых метрик. | backend/api/system_metrics.py — /metrics, Prometheus: http_requests_total, http_errors_total, decide_requests_total, events_accepted_total, report_requests_total. | curl /metrics после запросов к API; проверить наличие счётчиков. | Запущенный backend. | `готово` |
| 45 | B9-4 | Структурированные логи. | backend/Runbook.md — формат JSON (timestamp, level, logger, message, module, funcName, lineno, exception). | Просмотр логов (например logs/all_logs.log) или вывод при запросе. | — | `готово` |
| 46 | B9-6 | Учёт нагрузки и роста данных. | backend/Runbook.md — лимиты, индексы, партиционирование, масштабирование. | Наличие раздела и соответствие коду (config, init.sql). | — | `готово` |
| 47 | B9-7 | Меры эффективности хранения/чтения. | init.sql: индексы на decisions(created_at, subject_id, flag_id), event_occurrences(timestamp, decision_id); отчёты по start/end. | Проверка индексов в схеме БД; отчёт с большим окном — запрос ограничен по датам. | — | `готово` |
| 48 | B10-1 | Автоматизированный линтинг. | `ruff check backend`, Makefile lint, .github/workflows/lint.yml. | Запуск ruff check backend из корня. | — | `готово` |
| 49 | B10-2 | Автоматизированное форматирование. | `ruff format backend`, Makefile format, CI format --check. | Запуск ruff format --check backend. | — | `готово` |

---

## Доп. фича: Autopilot Ramp-up (п. 10 ТЗ)

Трассировка по реализации и проверке:

| ID задания | ID критерия | Проблема/риск | Где реализовано | Как проверяется | Какие данные нужны | Статус |
|------------|-------------|---------------|-----------------|-----------------|--------------------|--------|
| 50 | FX-1 | Целевой сценарий: план → старт автопилота → gates → step_up/step_back, safety actions, лог решений. | `backend/autopilot_ramp/` (ramp_plan, ramp_state, ramp_evaluator, ramp_apply), API `autopilot_ramp_api.py`; вызов `sync_and_tick_autopilot` из decide. | Демо: создать ramp plan, запустить автопилот на running-эксперименте, проверить GET state и GET decision-log; при срабатывании guardrail — safety action (pause/rollback/step_back). Тесты `tests/test_autopilot_ramp_api.py`. | Эксперимент running, ramp_plan со ступенями и safety_actions; при необходимости guardrail для проверки safety. | см. ограничения ниже |
| 51 | FX-2 | Границы и риски допфичи должны быть описаны и согласованы с поведением. | `backend/autopilot_ramp/LOGIC.md` — цель, сущности, gates, safety actions, прозрачность, ручное вмешательство. Ограничения — в этом разделе и в backend/README «Ограничения и упрощения». | Сверка LOGIC.md и демо: режимы (autopilot/manual/paused), override только в manual, лог решений; явное перечисление того, что не реализовано (см. ниже). | — | готово при актуальной документации |

**Ограничения реализации Autopilot Ramp-up (для FX-1/FX-2):**

- **Реализовано:** ramp-plan (ступени, observation window), gate_data_sufficiency (min_total_impressions, min_impressions_per_variant, min_minutes_on_step), safety по срабатыванию guardrails (pause / rollback_to_control / step_back), лог решений (experiment_ramp_decision_log), режимы autopilot/manual/paused, override ступени только в manual, API плана/состояния/режима/override/decision-log.
- **Не реализовано / частично:** (1) Применение трафика: при step_up/step_back не обновляются `experiments.audience_fraction` и веса вариантов — фактическая доля трафика по ступеням не меняется без ручного PATCH эксперимента. (2) Gates: не проверяются gate_safety (error_rate_threshold, latency_p95_ms) и gate_data_health (require_no_srm, require_no_mass_rejected); учёт guardrails только через историю срабатываний (safety), без проверки порогов error/latency/DQ в самом проходе gates. (3) Уведомления (notify в safety_actions) не отправляются.

Для полного соответствия описанию 10.1–10.5 необходимо: реализовать в `ramp_apply.apply_ramp_step_to_experiment` обновление `experiments.audience_fraction` и весов вариантов по текущей ступени; при необходимости — проверки error_rate/latency/SRM/rejected в evaluator и расширение safety-триггеров.

--

## Доп. фича: Разруливание конфликтующих экспериментов (п. 11 ТЗ)

Трассировка по реализации и проверке (критерии FX-1, FX-2):

| ID задания | ID критерия | Проблема/риск | Где реализовано | Как проверяется | Какие данные нужны | Статус |
|------------|-------------|---------------|-----------------|-----------------|--------------------|--------|
| 52 | FX-1 | Конфликты явны до запуска; детерминированное решение при выдаче; корректная атрибуция и аудит. | **11.2.1** Один флаг — один активный эксперимент: `init.sql` — `idx_experiments_one_active_per_flag` (UNIQUE по flag_id WHERE status IN ('running','paused')); при старте `start_experiment` проверка на занятость флага. **11.2.2/11.2.3** Конфликт «на поверхности» и взаимоисключение: домены в `conflict_domains`, привязки в `experiment_conflict_bindings`. **11.3** Модель доменов: CRUD конфликтных доменов и привязок экспериментов — `backend/api/conflict_domains.py`, `backend/functions/conflicts.py` (list_domains, create_domain, upsert_binding и др.). **11.4** Политики: mutual_exclusion, bid, priority в `_pick_winner_for_domain` (conflicts.py). **11.5** Детерминизм: выбор победителя по `_stable_score` / `_stable_ratio` от seed `subject_id\|domain_key\|config_version\|...`. Preflight: `get_preflight_conflicts` при переходе в running, ответ с `conflict_warnings`. Decide: `resolve_experiment_conflicts` → проигравшие получают `default_value`, в ответе `conflict_lost` и `conflict_domain`. Аудит: `decision_conflict_log`, `store_conflict_logs`, отчёт — `conflict_stats` (times_winner, times_loser), API `GET .../conflict-log`. | Интеграционные автотесты `tests/test_conflicts_api.py`: CRUD доменов/привязок, mutual_exclusion (детерминизм для одного subject), priority (побеждает больший tier), bid (побеждает большая ставка), preflight-видимость и аудит через conflict-log. Дополнительно возможна ручная проверка через POST /decide и GET конфликтных endpoint’ов. | Два эксперимента на разных флагах, оба в одном conflict domain; субъект в таргетинге обоих. | готово |
| 53 | FX-2 | Границы и прозрачность: что гарантируется, что — только предупреждение, ограничения реализации. | Один активный эксперимент на флаг — жёстко (БД + проверка при start). Конфликты по доменам — при выдаче (decide) проигравшие не применяются; preflight при запуске — только предупреждение (запуск не блокируется). Документация: этот раздел матрицы и backend/README (при наличии раздела по конфликтам). | Сверка с ТЗ: взаимное исключение в домене выполнено; выбор по ставке и по приоритету реализован; детерминизм по subject_id+config_version; аудит и видимость проигравшего (conflict_lost, conflict_log, conflict_stats) обеспечены. | — | готово |

**Ограничения реализации (для FX-1/FX-2):**

- **Реализовано:** конфликт на одном флаге (один активный эксперимент на флаг); конфликтные домены (key, name, description, default_policy); привязки эксперимента к доменам (policy, priority_tier, bid_value, is_enabled); три политики (mutual_exclusion, bid, priority); детерминированный выбор победителя (SHA256-based tie-breaker и взвешенный выбор по bid); preflight при переходе в running (conflict_warnings в ответе); в decide — fallback на default для проигравших, поля conflict_lost и conflict_domain в ответе; лог конфликтов (decision_conflict_log), conflict_stats в отчёте, API conflict-log.
- **Не реализовано / ограничения:** Preflight не блокирует запуск — только предупреждает; при желании жёсткой блокировки запуска при конфликте в домене нужна отдельная проверка на стороне API.

--

## Доп. фича: Learnings Library — база знаний и похожие эксперименты (п. 9 ТЗ)

Трассировка по реализации и проверке (критерии FX-1, FX-2):

| ID задания | ID критерия | Проблема/риск | Где реализовано | Как проверяется | Какие данные нужны | Статус |
|------------|-------------|---------------|-----------------|-----------------|--------------------|--------|
| 54 | FX-1 | Без единой базы выводов команда повторяет эксперименты и теряет знания; без поиска похожих кейсов PM стартует "с нуля". | **Модель данных:** `docker/postgres/learnings_library.sql` — `experiment_learnings`, `learning_guardrails`, `experiment_learning_audit_log`, `learning_guardrails_audit_log`, `learning_similarity_cache`, индексы и FTS (`search_document`: название эксперимента, flag_key, hypothesis, notes, tags, платформы, страны и др.). **API:** `backend/api/learnings.py` — `GET /api/v1/learnings` (поиск + фильтры), `GET /api/v1/learnings/{id}`, `GET /api/v1/experiments/{id}/learning`, `PUT /api/v1/experiments/{id}/learning`, `GET /api/v1/learnings/{id}/audit`, `GET /api/v1/learnings/{id}/similar`; `backend/api/experiments.py` — policy-gate завершения. **Логика:** `backend/functions/learnings.py` — upsert, объединённый аудит, similarity-score с reasons (flag/metric/tags/platforms/countries/app_versions/targeting_summary/variant_structure/guardrails). **Контракты:** `backend/docs/schems.py` (Learning*Schema). | Интеграционные тесты `tests/test_learnings_api.py`: upsert+get по experiment/id, аудит изменений (в т.ч. guardrails), similar list + reasons, auth-check; policy-test в `tests/test_experiments_api.py` при `LEARNINGS_REQUIRED_ON_COMPLETE=true`; общая регистрация endpoint'ов в `backend/server.py`. | Эксперимент, флаг, авторизованный `experimenter`/`admin`, payload learning. Для similar — минимум 2 learning-записи с пересечением признаков. | готово |
| 55 | FX-2 | Без явных границ качества библиотека деградирует (нет обязательности заполнения и прозрачности ограничений). | Матрица и код фиксируют границы: структурированное хранение, фильтры, полнотекстовый поиск (в т.ч. по названию эксперимента), аудит learning и guardrails, выдача похожих с reasons, policy-gate при completion (LEARNINGS_REQUIRED_ON_COMPLETE). Similarity — по п. 9.4.1 «иные варианты, которые считаются адекватными» (эвристика по признакам). | Сверка ТЗ с реализацией: поля learning и аудит в API/DB; поиск по названию/гипотезе/заметкам/тегам через FTS; сценарий "похожие" через `GET /learnings/{id}/similar`; сценарий policy-gate при `POST /experiments/{id}/complete`. | — | готово |

**Реализовано по п. 9 ТЗ (FX-1, FX-2):**

- Структурированное хранение learning (гипотеза, primary metric, outcome/action, guardrails, контекст, ссылки, notes, completion-flag). Полнотекстовый поиск по названию эксперимента, гипотезе, заметкам, тегам, flag_key, платформам, странам и др. (триггер `set_experiment_learning_search_document`). Фильтры: ключ флага, команда/владелец, статус результата, дата, страны/платформы, основная метрика. Аудит изменений learning и guardrails (кто/когда). Похожие эксперименты: эвристический score по признакам (ключ флага, метрика, теги, платформы, страны, app_versions, targeting_summary, variant_structure, guardrails) и выдача reasons. Policy-gate: при `LEARNINGS_REQUIRED_ON_COMPLETE=true` завершение эксперимента без completed learning возвращает 409.
